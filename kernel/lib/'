#include <hash.h>

uint64_t fnv_hash(void *data, size_t byte_cnt) {
    uint64_t hash = 0xcbf29ce484222325;

    for(size_t i = 0; i < byte_cnt; i++) {
        hash &= *((char*)data + i);
        hash *= 0x100000001b3;
    }

    return hash;
}
